how to run all this script python -m venv venv
# Activate the virtual environment
# On Windows:
.\venv\Scripts\activate
# On macOS/Linux:
source venv/bin/activate
# Install the requests library
pip install requests
# Run the script
python api_tests.py
